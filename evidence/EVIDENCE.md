# Evidence

Generated by `npm run evidence`. Discovery runs (real LLM, Gemini via Vertex) are in the
`discovery-*` directories; everything below is deterministic replay with **no model in the loop**.

## Replay matrix

| Scenario | Expectation | Result status | Detail |
|---|---|---|---|
| lookup / happy path | success + balance in cents | `success` | {"savingsBalance":482355} |
| lookup / member not found | business_outcome MEMBER_NOT_FOUND | `business_outcome` | MEMBER_NOT_FOUND: No member found for member number "999999". |
| lookup / interstitial dialog | recoverable: dismiss + succeed | `success` | {"savingsBalance":482355} |
| lookup / application error | hard_failure APP_ERROR | `failure` | APP_ERROR @ s2 |
| lookup / session timeout (HITL reauth) | needs_intervention -> resumed -> success | `success` | {"savingsBalance":482355} |
| open-subaccount / happy path (approved, risky allowed) | success + new account number | `success` | {"newAccountNumber":"100123-92"} |
| open-subaccount / restricted member | business_outcome PERMISSION_DENIED | `business_outcome` | PERMISSION_DENIED: Permission denied. A member in status "restricted" is not pe |
| open-subaccount / below-minimum deposit | business_outcome VALIDATION_FAILED | `business_outcome` | VALIDATION_FAILED: The minimum opening deposit for Money Market is $2,500.00. |
| open-subaccount / CASCADE tenant (cross-tenant reuse) | success on a different tenant, no re-record | `success` | {"newAccountNumber":"100123-90"} |
| open-subaccount / UI drift, NO self-heal | hard_failure LOCATOR_UNRESOLVED where the ladder truly cannot resolve (brittle replays die here) | `failure` | LOCATOR_UNRESOLVED @ s6 |
| open-subaccount / UI drift, WITH self-heal | success — ladder absorbs the renamed link (href rung); self-heal recovers the renamed button + writes a patch | `success` | {"newAccountNumber":"100123-93"} |
| ParaBank (REAL public bank) / balance lookup, account 40650 | success — logs in (injected creds) and reads a live balance on a third-party bank | `success` | {"accountBalance":30050} |

Each row has a matching `replay-*` directory with `events.jsonl`, screenshots, and `summary.json`.
Failure/■ runs also include `failure.html`.
